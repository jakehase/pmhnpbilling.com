name: Public tree privacy guard

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  current-tree-privacy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reject private operational artifacts
        run: node scripts/public-tree-privacy-check.mjs
